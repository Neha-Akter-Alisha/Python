# We can convert a float type variable into a integer type by using type conversion
# Write a program to add two numbers asking users for inputs
n = int(input("Enter the first nunmber: "))
m = int(input("Enter the second nunmber: "))
result = int(m + n)
print("Result of addition is: ", result)